server:
  port: ${env.port:5022}
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${env.db.host:47.91.170.6}:${env.db.port:3306}/${env.db.database:facebook}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&useSSL=false&allowMultiQueries=true
    username: ${env.db.username:ods_select}
    password: ${env.db.password:ods_select}
#    type: com.alibaba.druid.pool.DruidDataSource
#    druid:
#      filters: config
#      connection-properties: druid.stat.mergeSql=false;druid.stat.slowSqlMillis=3000;config.decrypt=${env.db.encrypted:false};config.decrypt.key=${env.db.public-key:}